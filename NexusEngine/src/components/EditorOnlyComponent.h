#pragma once

namespace NexusEngine
{
    /// <summary>
    /// Marks an entity as editor-only so runtime views can ignore it and editor UI can hide it.
    /// </summary>
    struct EditorOnlyComponent
    {
    };
} // namespace NexusEngine
